---
aliases:
  - setup-postgres
description: "Set up Postgres skill for this project"
---
Run `project-skills postgres` in the project root (or `just skills postgres`). Copies postgres into `.skills/` and writes/updates `AGENTS.md` with skill references and routing hints. SQL routing hints are included automatically.
