---
aliases:
  - project-rust
  - setup-rust
description: "Set up Rust skills for this project"
---
Run `project-skills rust` in the project root (or `just skills rust`). This copies the Rust skills into `.skills/` and writes/updates `AGENTS.md` with skill references and routing hints.
