---
aliases:
  - tf
description: "Terraform best practices"
---
Follow Terraform best practices. Use modules for reusable infrastructure. Always use remote state with locking. Pin provider versions. Use locals for repeated expressions. Tag all resources consistently. Separate environments via workspaces or directory structure. Run `terraform fmt` and `terraform validate` before applying.
