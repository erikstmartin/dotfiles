---
aliases:
  - setup-ruby
  - setup-rails
description: "Set up Ruby/Rails skills for this project"
---
Run `project-skills ruby` in the project root (or `just skills ruby`). Copies ruby-rails into `.skills/` and writes/updates `AGENTS.md` with skill references and routing hints.
