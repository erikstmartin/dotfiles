---
aliases:
  - project-frontend
  - setup-frontend
description: "Set up frontend skills for this project"
---
Run `project-skills frontend` in the project root (or `just skills frontend`). This copies the frontend skills into `.skills/` and writes/updates `AGENTS.md` with skill references and routing hints.
