---
aliases:
  - project-audio
  - setup-audio
description: "Set up audio/JUCE/DSP skills for this project"
---
Run `project-skills audio` in the project root (or `just skills audio`). This copies the audio skills into `.skills/` and writes/updates `AGENTS.md` with skill references and routing hints.
