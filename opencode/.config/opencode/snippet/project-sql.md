---
aliases:
  - setup-sql
description: "Set up SQL query/schema skills for this project (engine-agnostic)"
---
Run `project-skills postgres` or `project-skills sqlite` in the project root — sql routing hints are included automatically. Use `project-skills sql` only if you want the routing hints without an engine-specific skill.
