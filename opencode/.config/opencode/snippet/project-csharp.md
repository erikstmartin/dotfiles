---
aliases:
  - setup-csharp
  - setup-dotnet
description: "Set up C#/.NET skills for this project"
---
Run `project-skills csharp` in the project root (or `just skills csharp`). Copies csharp into `.skills/` and writes/updates `AGENTS.md` with skill references and routing hints.
