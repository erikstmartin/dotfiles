---
aliases:
  - project-godot
  - setup-godot
description: "Set up Godot/game dev skills for this project"
---
Run `project-skills godot` in the project root (or `just skills godot`). This copies the Godot skills into `.skills/` and writes/updates `AGENTS.md` with skill references and routing hints.
