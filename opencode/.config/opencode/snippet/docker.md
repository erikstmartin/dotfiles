---
aliases:
  - dockerfile
description: "Docker best practices"
---
Follow Docker best practices. Use multi-stage builds to minimize image size. Pin base image versions. Run as non-root user. Use .dockerignore. Prefer COPY over ADD. One process per container. Use HEALTHCHECK. Layer cache: put rarely-changing steps first.
