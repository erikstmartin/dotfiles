---
aliases:
  - project-infra
  - setup-infra
description: "Set up infra skills for this project"
---
Run `project-skills infra` in the project root (or `just skills infra`). This copies the infra skills into `.skills/` and writes/updates `AGENTS.md` with skill references and routing hints.
