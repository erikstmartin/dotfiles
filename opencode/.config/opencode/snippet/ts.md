---
aliases:
  - typescript
  - js
description: "TypeScript idioms and conventions"
---
Use strict TypeScript. Prefer `const` over `let`. Use proper types — avoid `any`. Prefer `interface` for object shapes, `type` for unions/intersections. Use async/await over raw promises. Handle errors explicitly.
