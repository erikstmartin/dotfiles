---
aliases:
  - setup-sqlite
description: "Set up SQLite skill for this project"
---
Run `project-skills sqlite` in the project root (or `just skills sqlite`). Copies sqlite into `.skills/` and writes/updates `AGENTS.md` with skill references and routing hints. SQL routing hints are included automatically.
